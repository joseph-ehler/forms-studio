name: LocationField
type: composite
specVersion: "1.0"
description: Geographic coordinates with latitude and longitude inputs
metadata:
  batch: 6
  difficulty: complex
  added: "2025-10-23"
composite:
  layout: row
  gap: normal
  parts:
    - name: lat
      type: number
      label: Latitude
    - name: lng
      type: number
      label: Longitude
value:
  default: "{ lat: 0, lng: 0 }"
  structure: composite
aria:
  live: polite
  invalid: hasError
  describedby: description
facade:
  enabled: true
  removal: v2.0.0
  codemod: pnpm codemod:fields
