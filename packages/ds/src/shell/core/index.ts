/**
 * Shell Core - Environment + State (Layers 3 & 4)
 * 
 * Single source of truth for device capabilities and shell state.
 */

// Layer 3: Environment (device detection, test overrides)
export * from './environment';

// State management
export * from './state';
