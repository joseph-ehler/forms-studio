/**
 * Route Shells (Layer 6: Recipes)
 * 
 * URL-bound recipes built on shells + routing logic.
 * 
 * Phase 1: Directory created
 * Phase 3 (optional): Move from /routes to here
 * 
 * Planned:
 * - FullScreenRouteShell - ModalShell + routing guards
 * - FlowScaffoldShell - PageShell + multi-step state + URL sync
 * - RoutePanelShell - DrawerShell + routing + persistence
 */

// Empty for now - routes will be migrated in Phase 3
export {};
