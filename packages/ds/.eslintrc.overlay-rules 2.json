{
  "$schema": "https://json.schemastore.org/eslintrc",
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["react-day-picker/dist/style.css"],
            "message": "❌ Do NOT import DayPicker CSS directly. Use CalendarSkin component instead, which imports ds-calendar.css."
          }
        ],
        "paths": [
          {
            "name": "react-day-picker",
            "importNames": ["DayPicker"],
            "message": "❌ Do NOT import DayPicker directly. Use CalendarSkin from @wizard-react/overlay instead."
          }
        ]
      }
    ],
    "no-restricted-syntax": [
      "error",
      {
        "selector": "JSXAttribute[name.name='className'][value.value=/flex-1.*min-h-0.*overflow-auto/]",
        "message": "❌ Do NOT manually build scroll containers. Use OverlayPicker's content/header/footer slots instead."
      },
      {
        "selector": "JSXAttribute[name.name='style'] > JSXExpressionContainer > ObjectExpression > Property[key.name='position'][value.value='fixed']",
        "message": "❌ Do NOT manually position overlays. Use OverlayPicker primitive instead."
      },
      {
        "selector": "CallExpression[callee.object.name='document'][callee.property.name='addEventListener'][arguments.0.value='mousedown']",
        "message": "⚠️  Prefer 'pointerdown' over 'mousedown' for better touch/pen support."
      }
    ]
  },
  "overrides": [
    {
      "files": ["**/overlay/**/*.tsx", "**/overlay/**/*.ts"],
      "rules": {
        "no-restricted-imports": "off"
      }
    }
  ]
}
